public class Main {

    public static void main(final String[] args) throws Exception {
//        System.out.println("Hibernate tutorial");
//
//        Session session = AppSessionFactory.getSessionFactory().openSession();
//
//        Objects.requireNonNull(session);
//
//        session.beginTransaction();

//        CarEntity carEntity = session.get(CarEntity.class, 2);
//
//        System.out.println(carEntity.getName());
//        List result = session.createQuery("from CarEntity", CarEntity.class).list();
//        for (CarEntity car : (List<CarEntity>) result ) {
//            System.out.println(car.getId() + ", " + car.getName());
//        }

//        CarEntity contactEntity = new CarEntity();
//        contactEntity.setId(10);
//        contactEntity.setName("Lada");
//
//        session.save(contactEntity);
//        session.getTransaction().commit();

//        session.close();
    }
}